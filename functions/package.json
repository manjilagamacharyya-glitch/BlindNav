{
  "name": "blind_nav_functions",
  "description": "Firebase Cloud Functions for BlindNav safety and SOS features.",
  "scripts": {
    "lint": "echo 'Lint skipped for hackathon'",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.4.0"
  },
  "engines": {
    "node": "20"
  },
  "devDependencies": {
    "eslint": "^10.2.1"
  }
}
