/// Firestore integration is planned for the next implementation stage.
///
/// At the moment, Firebase Auth and Cloud Functions are the main backend
/// services included in the starter version.
class FirestoreService {
  FirestoreService._();
}
